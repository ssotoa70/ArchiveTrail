-- ArchiveTrail: Schema Creation
-- Run this first to create the database schema in VAST DB.
-- Execute via VAST DB query editor or any Trino-compatible SQL client.

CREATE SCHEMA IF NOT EXISTS vast."archive/lineage";
